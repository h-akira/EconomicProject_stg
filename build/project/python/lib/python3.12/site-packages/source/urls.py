from . import views
from had.urls import path

urlpatterns = [
  path("score", views.score, name="score", methods=["GET"], login_required=True),
  path("normal/yahoo/{symbol}", views.yahoo, name="yahoo", methods=["GET"], login_required=True),
]

