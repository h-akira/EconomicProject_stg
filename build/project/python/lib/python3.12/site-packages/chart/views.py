from had.shourtcuts import render, redirect, error_render, RenderSettings, reverse
from project import settings
# try:
#   import boto3
#   from botocore.exceptions import ClientError
#   from boto3.dynamodb.conditions import Key, Attr
# except ModuleNotFoundError:
#   pass
#
# table_name = "table-wiki-stg"

def score(request):
  context = {
    "source_path": reverse("source:score")
  }
  return render(request, 'chart/score.html', context)

