from . import views
from had.urls import path

urlpatterns = [
  path("test", views.test, name="test", methods=["GET"], login_required=True),
  path("score", views.score, name="score", methods=["GET"], login_required=True),
  path("normal", views.normal, name="normal", methods=["GET"], login_required=True)
]

